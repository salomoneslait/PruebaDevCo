apiVersion: v1
kind: Namespace
metadata:
  name: pruebadevco

---

apiVersion: apps/v1
kind: Deployment 
metadata:
  name: test-devco
  namespace: pruebadevco
spec:
  selector:
    matchLabels:
      app: test-devco
  replicas: 2 
  template:
    metadata:
      labels:
        app: test-devco
    spec:
      containers:
        - name: imagen-prueba
          imagePullPolicy: Always
          image: salomoneslait/prueba-devco@sha256:9ab2d674e7dd1483bf3f78a6a2322d038c71df81d09cf3f7cc57318b13a1c14d
          resources:
            requests:
              cpu: 500m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 10001
            readOnlyRootFilesystem: true
            capabilities:
              drop:
               - NET_RAW
               - ALL
          ports:
            - containerPort: 3000

---

apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-devco
  namespace: pruebadevco
spec:
  type: LoadBalancer
  selector:
    app: test-devco
  ports:
  - name: http
    port: 80
    targetPort: 3000